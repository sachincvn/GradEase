import 'package:flutter/material.dart';

class ManageStudendsScreen extends StatelessWidget {
  const ManageStudendsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
          child: Column(
        children: [],
      )),
    );
  }
}
